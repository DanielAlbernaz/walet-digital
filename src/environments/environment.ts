export const environment = {
  production: true,
  // Produção / Vercel (ng build usa este arquivo): backend exposto via ngrok.
  // Domínio fixo do ngrok — não muda mais ao reiniciar o túnel.
  baseApiUrl: 'https://camren-ubiquitous-challengingly.ngrok-free.dev/api/',
};
